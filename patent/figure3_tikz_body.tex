\begin{tikzpicture}[
  node distance=10mm and 6mm,
  every node/.style={font=\small},
  flow tag/.style={font=\small, inner sep=1pt}
]

% ===== 第一层：三个并行监测（同宽） =====
\node[flow box, minimum width=24mm] (kfield)
  {关键手术野\\判别结果 $p_{cv}$};
\node[flow box, minimum width=24mm, right=of kfield] (conf)
  {任务分类\\置信度 $p_{un}$};
\node[flow box, minimum width=24mm, right=of conf] (stagn)
  {任务进度\\停滞检测 $p_{prg}$};

% ===== 第二层：判定菱形（左上 / 中间 / 右下错落） =====
\node[flow decision, below=5mm of kfield, minimum width=14mm] (c1)
  {$\lnot\mathrm{CritView}(p_{cv})$?};
\node[flow decision, below=8mm of conf, minimum width=14mm] (c2)
  {$\lnot\mathrm{Confidence}(p_{un})$?};
\node[flow decision, below=12mm of stagn, minimum width=14mm] (c3)
  {$\mathrm{ProgStall}(p_{prg})$?};

\draw[flow arrow] (kfield) -- (c1);
\draw[flow arrow] (conf) -- (c2);
\draw[flow arrow] (stagn) -- (c3);

% ===== 三个判别结果分别直接连接异常事件触发框 =====
\coordinate (trigTop) at ($(conf.center |- c3.south)+(0,-6mm)$);

% ===== 主列：触发判定与分流（14mm 等距） =====
\node[flow box, minimum width=76mm, align=center, anchor=north] (trigger)
  at (trigTop)
  {异常事件触发\\[1pt]
   $\mathrm{Abn}(p_{cv},p_{un},p_{prg})
   =\lnot\mathrm{CritView}(p_{cv})$\\
   $\lor\lnot\mathrm{Confidence}(p_{un})
   \lor\mathrm{ProgStall}(p_{prg})$};

\draw[flow arrow] (c1.south) -- (c1.south |- trigger.north);
\draw[flow arrow] (c2.south) -- (c2.south |- trigger.north);
\draw[flow arrow] (c3.south) -- (c3.south |- trigger.north);

\node[flow decision, below=10mm of trigger, minimum width=14mm] (dec)
  {触发\\异常?};

\draw[flow arrow] (trigger.south) -- (dec.north);

% ===== 否：右侧 → 继续监测（紧凑间距） =====
\node[flow box, minimum width=24mm, right=10mm of dec] (monitor)
  {继续监测};

\draw[flow arrow] (dec.east) -- (monitor.west);
\node[flow tag, anchor=south]
  at ($(dec.east)!0.5!(monitor.west)+(0,1.2mm)$) {否};

% ===== 是：下方横总线 + 三根竖直支线 =====
\coordinate (hbus) at ($(dec.south)+(0,-8mm)$);

\draw[line width=0.6pt] (dec.south) -- (hbus);
\node[flow tag, anchor=east]
  at ($(dec.south)!0.42!(hbus)+(-2.5mm,0)$) {是};

\node[flow box, rounded corners=2mm, minimum width=28mm]
  (out1) at ($(hbus)+(-34mm,-12mm)$)
  {人工接管指令};
\node[flow box, minimum width=28mm] (out2) at ($(hbus)+(0,-12mm)$)
  {异常类型判别};
\node[flow box, rounded corners=2mm, minimum width=28mm]
  (out3) at ($(hbus)+(34mm,-12mm)$)
  {进入异常\\恢复图};

\coordinate (hleft) at (out1.north |- hbus);
\coordinate (hmid) at (out2.north |- hbus);
\coordinate (hright) at (out3.north |- hbus);

\draw[line width=0.5pt] (hleft) -- (hright);
\draw[flow arrow] (hleft) -- (out1.north);
\draw[flow arrow] (hmid) -- (out2.north);
\draw[flow arrow] (hright) -- (out3.north);

\foreach \src/\tgt/\lbl in {
  hleft/out1/$\lnot\mathrm{CritView}$,
  hmid/out2/$\lnot\mathrm{Confidence}$,
  hright/out3/$\mathrm{ProgStall}$} {
  \node[flow tag, anchor=east]
    at ($(\src)!0.52!(\tgt.north)+(-0.8mm,0)$) {\lbl};
}

% ===== 异常类型判别：下方三支分流 =====
\coordinate (typeBus) at ($(out2.south)+(0,-8mm)$);

\draw[line width=0.6pt] (out2.south) -- (typeBus);

\node[flow box, minimum width=24mm] (type1) at ($(typeBus)+(-26mm,-12mm)$)
  {器械意外\\触碰组织};
\node[flow box, minimum width=24mm] (type2) at ($(typeBus)+(0,-12mm)$)
  {组织从夹爪\\脱落};
\node[flow box, minimum width=24mm] (type3) at ($(typeBus)+(26mm,-12mm)$)
  {未知异常};

\coordinate (tleft) at (type1.north |- typeBus);
\coordinate (tmid) at (type2.north |- typeBus);
\coordinate (tright) at (type3.north |- typeBus);

\draw[line width=0.5pt] (tleft) -- (tright);
\draw[flow arrow] (tleft) -- (type1.north);
\draw[flow arrow] (tmid) -- (type2.north);
\draw[flow arrow] (tright) -- (type3.north);

\end{tikzpicture}
